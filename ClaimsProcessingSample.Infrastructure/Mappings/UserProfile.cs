using AutoMapper;
using ClaimsProcessingSample.Application.Models.Identity;
using ClaimsProcessingSample.Application.Responses.Identity;

namespace ClaimsProcessingSample.Infrastructure.Mappings
{
    public class UserProfile : Profile
    {
        public UserProfile()
        {
            CreateMap<UserResponse, BlazorHeroUser>().ReverseMap();
            CreateMap<ChatUserResponse, BlazorHeroUser>().ReverseMap()
                .ForMember(dest => dest.EmailAddress, source => source.MapFrom(source => source.Email)); //Specific Mapping
        }
    }
}